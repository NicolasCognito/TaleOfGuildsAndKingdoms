using System.Collections;
using System.Collections.Generic;
using Sirenix.OdinInspector;
using UnityEngine;

[System.Serializable]
public class LaborerSlotModel
{
    //required qualification (as string)
    public string qualification;

    
    /* //danger level (0 to 100)
    private int _danger;
    public int danger
    {
        get { return _danger; }
        set { _danger = Mathf.Clamp(value, 0, 100); }
    }

    //attractiveness (0 to 100)
    private int _appeal;
    public int appeal
    {
        get { return _appeal; }
        set { _appeal = Mathf.Clamp(value, 0, 100); }
    } */
}
